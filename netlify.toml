[build]
  command = "pnpm run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes so React Router / hash links work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
